We have spent several hours debugging the WhatsApp Cloud API integration and have isolated the issue. I want you to carefully review the implementation and determine if there is ANY difference between the request generated by my code and the request that succeeds/fails with curl.

Facts we have proven:

1. The backend is running correctly.
2. PM2 is using the production System User token.
3. The WABA ID is:
1546864506819232

4. The Phone Number ID is:
1273577662499310

5. The System User token has these permissions:
- business_management
- whatsapp_business_management
- whatsapp_business_messaging
- whatsapp_business_manage_events
- public_profile

Verified using:
GET /me/permissions

6. Business Verification is complete.
7. WABA review status is APPROVED.
8. Phone number is Connected.
9. Listing templates works.
10. Creating Utility templates via Graph API works.

Example:
POST /{WABA_ID}/message_templates
category = UTILITY

returns:
status = REJECTED (expected because of template content)
NOT a permission error.

However...

Creating an Authentication template always returns:

HTTP 400
OAuthException
code = 10
subcode = 2388185

Application does not have permission for this action.
This WhatsApp Business account does not have permission to create message template.

The SAME error occurs:

1. From my backend
2. From direct curl
3. Using the same production token

Backend log:

[template/auth] PRE-REQUEST

{
"name":"login_otp_v1",
"category":"AUTHENTICATION",
"language":"en_US",
"components":[
{
"type":"BODY",
"add_security_recommendation":true
},
{
"type":"FOOTER",
"code_expiration_minutes":5
},
{
"type":"BUTTONS",
"buttons":[
{
"type":"OTP",
"otp_type":"COPY_CODE",
"text":"Copy Code"
}
]
}
]
}

Meta returns:

Application does not have permission for this action.

Tasks:

1. Inspect every Authentication template request generated by the backend.
2. Compare it against Meta's official Authentication Template API specification.
3. Verify every field, endpoint, Graph API version, headers, JSON structure and component ordering.
4. Check whether our implementation differs from the official Meta documentation in any way.
5. Check if there are known changes in Graph API v22/v23 for Authentication templates.
6. If the implementation is correct, explicitly state that this is a Meta-side restriction or account capability issue and not an application bug.
7. If you find any difference from the official documentation, provide the exact code changes required.

Do not guess. Compare against the latest official Meta documentation and explain exactly why Authentication template creation fails while Utility template creation succeeds.