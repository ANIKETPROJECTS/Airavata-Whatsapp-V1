I am implementing WhatsApp Cloud API template management using the Meta Graph API.

There are two issues that need to be fixed.

Issue 1:
Authentication templates are failing with the error:

component of type BODY has unexpected field(s) (text)

Currently my backend uses the same payload structure for Utility, Marketing, and Authentication templates. This is incorrect because Authentication templates use a different payload and must not contain BODY.text.

Please modify the backend so that:

- Utility templates continue using the existing payload.
- Marketing templates continue using the existing payload.
- Authentication templates use the latest WhatsApp Cloud API Authentication Template specification.
- Do not send BODY.text for Authentication templates.
- Support OTP templates, security recommendation, code expiration, and copy-code button where supported.
- Validate the payload before sending it to Meta.

Issue 2:
Every template that I create successfully is stuck in the "Pending Review" / "In Review" state indefinitely.

For example, even a very simple template like:

Category: Utility
Body:
Hello {{1}}, this is a test utility message.

never gets approved and remains in review.

Please investigate why this happens.

Things to verify:

1. Confirm that the application is using the production:
   - Permanent System User Access Token
   - META_PHONE_NUMBER_ID
   - META_WABA_ID

2. Verify that the template creation request is being sent to the correct endpoint:

POST /{WABA_ID}/message_templates

using the production WABA ID.

3. Log the complete request body sent to Meta.

4. Log the complete response from Meta, including template ID and status.

5. Verify that the correct Graph API version is being used.

6. Check whether any required fields are missing or incorrectly formatted.

7. Verify that the access token has:
   - whatsapp_business_management
   - whatsapp_business_messaging

8. Confirm that templates are being created under the correct WhatsApp Business Account and not a test account.

9. Add detailed logging so I can see:
   - Request payload
   - Response payload
   - Meta error messages
   - Template ID
   - Initial template status

Please use the latest Meta WhatsApp Cloud API documentation and update the backend to fully support Utility, Marketing, and Authentication templates.