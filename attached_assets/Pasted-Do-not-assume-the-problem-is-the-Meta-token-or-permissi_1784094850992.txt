Do not assume the problem is the Meta token or permissions.

We have already verified the following manually:

1. The System User token is valid.
   GET /me returns:
   {
     "name": "Employee"
   }

2. The token can successfully access the production WABA.

   GET /1546864506819232/message_templates

   returns the approved "hello_world" template.

Therefore the token CAN access the production WhatsApp Business Account.

The issue only occurs when creating a template.

I want you to debug this by proving exactly what request is being sent to Meta.

Do NOT speculate.

Please do the following:

1. Find the exact function that creates a message template.

2. Immediately before the POST request, log:

- Graph API URL
- WABA ID
- Authorization token (only first 20 characters)
- Complete JSON payload

Example:

URL:
https://graph.facebook.com/v23.0/<WABA_ID>/message_templates

WABA:
1546864506819232

Token:
EAAS0h8ZCJ7eIBR9DU...

Payload:
{ ... }

3. Log the COMPLETE Meta response.

If Meta returns an error, print:

- HTTP status
- error.code
- error.type
- error.message
- error.error_subcode
- fbtrace_id
- entire response body

Do NOT replace Meta's response with a custom message.

I want the exact response from Meta.

4. Verify that the POST request uses:

process.env.META_ACCESS_TOKEN
process.env.META_WABA_ID

and not a hardcoded token or the old Test WABA ID.

5. Search the entire project for:

message_templates
META_ACCESS_TOKEN
META_WABA_ID
Authorization
Bearer
graph.facebook.com

and verify every location uses the production values.

6. If the backend catches Axios/fetch errors, remove any generic error replacement so the real Meta error is returned.

7. If you find multiple template creation functions, identify which one is actually used by the frontend.

Finally, explain exactly why template creation fails.

Do not guess. Base the conclusion only on the actual logs and Meta's exact response.